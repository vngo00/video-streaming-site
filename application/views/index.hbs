<main>
    <div id="product-list" class="product-list">
        {{#if posts}}
            {{#each posts}}
                {{> card this}}
            {{/each}}
        {{/if}}
    </div>
</main>
        
    